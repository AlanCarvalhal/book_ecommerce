services:
  api:
    build:
      context: .
      target: development
    container_name: book_ecommerce_api
    ports:
      - '3000:3000'
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - PORT=3000
      - NODE_ENV=development
